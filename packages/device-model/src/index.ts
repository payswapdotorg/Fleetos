/** @fleetos/device-model — module placeholder established by W001.
 * Real contracts arrive with their owning work item; do not import
 * internals across module boundaries (see tools/check-ownership.mjs). */
export const MODULE_NAME = "device-model" as const;
export const MODULE_VERSION = "0.1.0" as const;
