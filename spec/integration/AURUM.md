# Aurum Integration Contract

FleetOS owns operational truth. Aurum provides organizational communication/intelligence.

FleetOS may emit maintenance notices, incident warnings, approval requests, recovery messages, procurement updates and manager briefings.

Aurum returns delivery/outcome metadata and cannot mutate FleetOS truth except through explicitly authorized FleetOS action APIs.