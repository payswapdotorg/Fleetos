# apps/web/shell

Tech-Lead-owned UI shell territory.

This directory will host the control-plane web app shell (Next.js app root,
navigation, layout, cross-surface journey composition) in a later wave
(W061 Control Tower/journey convergence). For W001, this directory is
intentionally empty except for this README.

Workers MUST NOT modify this directory without explicit Tech Lead
reassignment (see `spec/worker-ownership.yaml` and `AGENTS.md`).
